Write a type vector3d (representing coordinates in a 3D space) as a literal type, so that the
following program is valid.

```cpp
int main() {
    constexpr vector3d
    std::cout << v1 <<
    constexpr vector3d
    std::cout << v2 <<
}
```